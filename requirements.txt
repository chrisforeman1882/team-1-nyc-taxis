# Dev tooling
pre-commit
pre-commit-hooks==6.0.0
ruff==0.15.5
detect-secrets==1.5.0

# Testing
pytest

# Data processing
# Note: databricks-connect is NOT installed here — it ships a modified pyspark
# that blocks local SparkSession creation, breaking unit tests.
# Unit tests use standard pyspark locally; integration tests run on Databricks
# where the session is provided by the cluster kernel.
pyspark==3.5.*

# ML & analytics
scikit-learn
pandas
matplotlib
